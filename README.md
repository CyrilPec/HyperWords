# HyperWords
Create a cross-domain compositional words hierarchy - structured representation. The key issue is: create abstract levels of the objects, not their instances.

Add .yaml file with short description, attributes of an object and methods which can object provide.
In the next same name folder-level create the next abstract object which exists in the Parent object.
